const password = prompt('your passward'); // prompt() is function provided by the browser which will prompt the user for input.



if (password === 'Bismillah') {
    console.log('Bismillah works')  
} else if (password === 'bissmillah') {
    console.log('bismillah works')
} else {
    console.log('Acccess not granted')
}

// for loop is another control structure.

const hobbies = ['sport', 'cooking'];

for (const hobby of hobbies) {
    console.log(hobbyww);
}