// in js we can perform math. e.g
console.log(10 * 9);
// we can use + for adding and concatenating e.g 
console.log('hrr' + 'fd')
console.log(2 + 7) //all others too
// === used for cheking equlity, we also use <,>, <=, >=.
